# Pleasanton Math Tournament Website

Website for the Pleasanton Math Tournament.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`
3. Build for production:
   `npm run build`
